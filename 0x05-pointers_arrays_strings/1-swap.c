#include "main.h"
/**
 * swap_int - the value of two integers swapped
 * @a: swapping integer
 * @b: swapping integer
 * sementics are of local variable
 */

void swap_int(int *a, int *b)
{
	int g;

	g = *a;
	*a = *b;
	*b = m;

}
