#include <stdio.h>
/**
 * main - the main function include an empty entry
 * Return: 0 null (completed)
 */
int main(void)
{
	int r;

	r = 0;
	while
		(r < 10)
		{
			putchar(r + '0');
			r++;
		}
		/* r takes the increment until the codition satisfy */
	putchar('\n');
	return (0);
}
