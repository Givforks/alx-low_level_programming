#include <stdio.h>
/**
 * main - A main pgrogram with entry point function
 * Return: 0 value return (completed)
 */
int main(void)
{
	int k;

	for (k = 0; k < 10; k++)
	       printf("%d", k);
	/* making use of format specifier */
	putchar('\n');
return (0);
}
