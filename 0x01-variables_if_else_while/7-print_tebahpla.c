#include <stdio.h>
/**
 * main - modulus program function for entry point value
 * Return: 0 void return type (completed)
 */
int main(void)
{
	char B;

	B = 'z';
	while
		(B >= 'a')
		{
			putchar(B);
			B--;
		}
	/* decrement reserve print lower case */
	putchar('\n');
	return (0);
}
